<div class="d-flex flex-column align-items-center justify-content-center h-75 p-2">
  <div class="card text-center shadow-lg w-50">
    <div class="card-body">
      <h5 class="card-title fs-2">Answer Wisely 🤔</h5>
      {{#if question}}
        <p
          class="card-text fs-3"
          data-question-id={{question.id}}
          id="question"
        >{{question.question}}</p>
        <textarea class="form-control text-center mb-3 fs-4" name="" id="user-answer"></textarea>
        <div class="mt-2">
          <button class="btn btn-danger mx-2 fs-5" id="abandon">Abandon</button>
          <button class="btn btn-success mx-2 fs-5" id="submit">Submit</button>
        </div>
        
      {{else}}
        <p>no questions</p>
      {{/if}}
    </div>
  </div>
  {{#if question}}
    <button type="button" class="btn btn-primary btn-lg my-4" id="newQuestion">Get new question</button>
  {{/if}}
</div>